import React from 'react';
import {type Props} from './type';
import CaseView from './CaseView';

const CaseContainer = (props: Props) => {
  return <CaseView {...props} />;
};

export default CaseContainer;
