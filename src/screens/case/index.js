/**
 * @format
 * @flow
 */

import React from 'react';
import {type Props} from './types';
import CaseContainer from './CaseContainer';

const Case = (props: Props) => {
  return <CaseContainer {...props} />;
};

export default Case;
