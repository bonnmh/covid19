/**
 * @format
 * @flow
 */

import React from 'react';
import SplashContainer from './SplashContainer';
import {type Props} from './types';

const Splash = (props: Props) => {
  return <SplashContainer {...props} />;
};

export default Splash;
