/**
 * @format
 * @flow
 */

import React from 'react';
import {type Props} from './types';
import AppScreenView from './AppScreenView';

const AppScreenContainer = (props: Props) => {
  return <AppScreenView {...props} />;
};

export default AppScreenContainer;
