/**
 * @format
 * @flow
 */

import React from 'react';
import AppScreenContainer from './AppScreenContainer';
import {type Props} from './types';

const AppScreen = (props: Props) => {
  return <AppScreenContainer {...props} />;
};

export default AppScreen;
